<%= form_for(movie, html: { multipart: true }) do |f| %>
  <% if movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
      <% movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-lg-6">
    <div class="field">
      <%= f.label :title, 'Tytuł' %>
      <%= f.text_field :title %>
    </div>

    <div class="field">
      <%= f.label :duration, 'Czas trwania' %>
      <%= f.number_field :duration %>
    </div>

    <div class="field">
      <%= f.label :genre, 'Gatunek' %>
      <%= f.text_field :genre %>
    </div>

    <div class="field">
      <%= f.label :pegi, 'Ograniczenie wiekowe' %>
      <%= f.number_field :pegi %>
    </div>

    <div class="field">
      <%= f.label :description, 'Opis' %>
      <%= f.text_field :description %>
    </div>

    <div class="field">
      <%= f.label :director, 'Reżyser' %>
      <%= f.text_field :director %>
    </div>
  </div>

  <div class="col-lg-6">
    <div class="field">
      <%= f.label :actors, 'Obsada' %>
      <%= f.text_field :actors %>
    </div>

    <div class="field">
      <%= f.label :country, 'Produkcja' %>
      <%= f.text_field :country %>
    </div>

    <div class="field">
      <%= f.label :premiere, 'Premiera' %>
      <%= f.date_select :premiere %>
    </div>

    <div class="field">
      <%= f.label :url_trailer, 'Url trailer' %>
      <%= f.text_field :url_trailer %>
    </div>

    <div class="field">
      <%= f.label :picture, 'Okładka' %>
      <%= f.file_field :picture %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
  </div>

<% end %>
