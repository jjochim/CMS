<%= form_for(room) do |f| %>
  <% if room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, 'Nazwa' %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :rows, 'Wiersze' %>
    <%= f.number_field :rows %>
  </div>

  <div class="field">
    <%= f.label :columns, 'Kolumny' %>
    <%= f.number_field :columns %>
  </div>

  <div class="actions">
    <%= f.submit value: 'Akceptuj', id: 'cms-room-create' %>
  </div>
<% end %>
