<%= form_for(seating) do |f| %>
  <% if seating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(seating.errors.count, "error") %> prohibited this seating from being saved:</h2>

      <ul>
      <% seating.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :slot %>
    <%= f.check_box :slot %>
  </div>

  <div class="field">
    <%= f.label :room_id %>
    <%= f.number_field :room_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
