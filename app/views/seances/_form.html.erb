<%= form_for(seance) do |f| %>
  <% if seance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(seance.errors.count, "error") %> prohibited this seance from being saved:</h2>

      <ul>
      <% seance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_date, 'Czas rozpoczęcia seansu' %>
    <%= f.datetime_select :start_date %>
  </div>

  <div class="field">
    <%= f.label :movie_id, 'Film' %>
    <%= f.collection_select :movie_id, Movie.all, :id, :title %>
  </div>

  <div class="field">
    <%= f.label :room_id, 'Sala' %>
    <%= f.collection_select :room_id, Room.all, :id, :name %>
  </div>

  <div class="field">
    <%= f.label :threed, '3D' %>
    <%= f.check_box :threed %>
  </div>

  <div class="field">
    <%= f.radio_button :lector, 'lektor', :checked => true %>
    <%= f.radio_button :subtitle, 'napisy', :checked => false %>
    <%= f.radio_button :dubbing, 'dubbing', :checked => false %>
  </div>

  <div class="actions">
    <%= f.submit value: 'Akceptuj' %>
  </div>
<% end %>
